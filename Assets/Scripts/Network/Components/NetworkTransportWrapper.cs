using System.Collections;
using UnityEngine;

namespace TDGame.Network.Components
{
    public class NetworkTransportWrapper : MonoBehaviour
    {

        // Use this for initialization
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}